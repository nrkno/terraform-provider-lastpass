# Build customization
builds:
  - goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
# Archive customization
archive:
  format: tar.gz
  files:
    - LICENSE
    - README.md
  format_overrides:
    - goos: windows
      format: zip
